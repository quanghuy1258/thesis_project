# Add Executable
INCLUDE_DIRECTORIES (${FFTW_INCULDE_DIR})
ADD_EXECUTABLE (main main.cpp)
TARGET_LINK_LIBRARIES (main eigen ${CMAKE_THREAD_LIBS_INIT} ${FFTW_LIB} ${CUDA_CUFFT_LIBRARIES})
ADD_DEPENDENCIES (main fftw)
