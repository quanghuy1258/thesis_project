# Add Executable
INCLUDE_DIRECTORIES (${FFTW_INCULDE_DIR})
AUX_SOURCE_DIRECTORY ("${CMAKE_SOURCE_DIR}/benchmark" BENCHMARKS)
ADD_EXECUTABLE (mainBenchmark ${BENCHMARKS})
TARGET_LINK_LIBRARIES (mainBenchmark benchmark eigen ${FFTW_LIB} ${CUDA_CUFFT_LIBRARIES})
ADD_DEPENDENCIES (mainBenchmark fftw)
